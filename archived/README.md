# Archived material

Legacy content kept for reference; active product docs live in `docs/`, runtime in `core/`, OpenClaw assets in `openclaw-knowledge/`.

- `game/` — former game bootstrap docs and pipeline
- `monitoring/` — old monitoring notes
- `scripts/` — meeting helper scripts (`check_meeting.py`, `spawn_meeting_agents.py`, …)
- `docs/` — one-off or hypothetical docs
