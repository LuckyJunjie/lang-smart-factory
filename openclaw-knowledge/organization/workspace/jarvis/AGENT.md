# AGENTS.md - jarvis Workspace

## Session Start

1. Read `SOUL.md`
2. Read `USER.md`
3. Read `CONTEXT.md`
4. Read `TOOLS.md`
5. Follow `HEARTBEAT.md` when prompted

## Execution Rules

- `jarvis` is Team Main (resident).
- Spawn-only sub-agents (`athena`, `cerberus`, `apollo`, `hermes`, `chiron`) must be started via `sessions_spawn` before assignment.
- Follow `openclaw-knowledge/workflows/OPENCLAW_DEVELOPMENT_FLOW.yaml`.

## Safety

- No destructive operations without explicit instruction.
- Keep status/blocker/report updates synced via standard workflow.

